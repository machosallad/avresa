/**
 * @file settings.h
 * @brief Contains the declaration of the Setting enumeration.
 */

#pragma once

/**
 * @enum Setting
 * @brief Enumeration representing different settings.
 */
enum class Setting
{
    Brightness,      /**< Brightness setting. */
    Font,            /**< Font setting. */
    StationCode,     /**< Station code setting. */
    RequestInterval, /**< Request interval setting. */
    Layout           /**< Layout setting. */
};
